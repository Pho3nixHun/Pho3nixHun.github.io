import { ContentChangesDirective } from './content-changes.directive';

describe('ContentChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentChangesDirective();
    expect(directive).toBeTruthy();
  });
});
